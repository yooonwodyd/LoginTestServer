spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/CommunityAPp
    username: root
    password: dbswodyd91
  jwt:
    secret: yoonwodyd
    expiration: 86400000
  jpa:
    open-in-view: false
    defer-datasource-initialization: true
    hibernate.ddl-auto: create
    show-sql: true
    properties:
      hibernate.format_sql: true
      hibernate.default_batch_fetch_size: 100
  sql.init.mode: always
